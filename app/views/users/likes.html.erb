<div class="container shadow">
  <div class="row">
    <h4 class="mb-5">いいねした献立</h4>
  </div>
  <div class="row">
    <% if @liked_menus.present? %>
      <% @liked_menus.each do |menu| %>
        <%= render 'menus/menu_card', menu: menu %>
      <% end %>
    <% else %>
      <p>いいねした献立がありません。</p>
    <% end %>
  </div>
</div>